apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: production-starter-ingress
spec:
  rules:
  - host: production-starter.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: production-starter-service
            port:
              number: 80